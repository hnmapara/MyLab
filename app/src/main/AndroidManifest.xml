<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lab.mapara.com.mylab">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".android.MyActivity"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".android.CalendarActivity"
            android:label="@string/title_activity_calendar">
            <!--<intent-filter>-->
            <!--<action android:name="android.intent.action.MAIN"/>-->

            <!--<category android:name="android.intent.category.LAUNCHER"/>-->
            <!--</intent-filter>-->
        </activity>
        <activity
            android:name=".android.RequestRectangleVisible"
            android:label="@string/title_activity_request_rectangle_visible">
        </activity>
        <activity
            android:name=".android.pageslie.FullscreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_fullscreen"
            android:theme="@style/FullscreenTheme">

        </activity>
        <activity
            android:name=".android.pageslie.FullscreenActivityNext"/>
        <activity android:name=".android.GraphActivity">

        </activity>
        <activity android:name=".android.AnimationActivity">
        </activity>

        <activity android:name=".android.likeanimation.LikeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>


    </application>
</manifest>
